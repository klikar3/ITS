<?php

use yii\helpers\Html;
use yii\helpers\ArrayHelper;
use yii\widgets\ActiveForm;

use app\models\rzKomponenten;
use app\models\RzSysteme;

/* @var $this yii\web\View */
/* @var $model app\models\rzServiceKomponenten */
/* @var $form yii\widgets\ActiveForm */
?>

<div class="rz-service-komponenten-form">

    <?php $form = ActiveForm::begin(); ?>

    <?= $form->field($model, 'id')->textInput(['disabled' => true]) ?>

    <?= $form->field($model, 'serviceId')->dropDownList(
            ArrayHelper::map(RzSysteme::find()->all(),'SId','System'),
            ['prompt'=>'Select...']
        ) ?>

    <?= $form->field($model, 'komponentId')->dropDownList(
            ArrayHelper::map(rzKomponenten::find()->all(),'id','name'),
            ['prompt'=>'Select...']
        ) ?>

    <div class="form-group">
        <?= Html::submitButton(Yii::t('app', 'Save'), ['class' => 'btn btn-success']) ?>
    </div>

    <?php ActiveForm::end(); ?>

</div>
